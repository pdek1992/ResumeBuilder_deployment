{
  "name": "resume-pdf-renderer",
  "version": "1.0.0",
  "description": "Headless Puppeteer PDF renderer for Resume Builder GHA pipeline",
  "private": true,
  "scripts": {
    "generate": "node generate-pdf.js",
    "verify": "node verify-pipeline.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "pako": "^2.1.0",
    "puppeteer": "^22.12.0"
  },
  "engines": {
    "node": ">=20"
  }
}
